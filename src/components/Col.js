import React from "react";

//is over is from when the user is about to drop it and the dragging is over. from the useDrop hooks
const Col = ({ isOver, children }) => {
	return <div>{children}</div>;
};

export default Col;
